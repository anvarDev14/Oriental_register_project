services:
  bot:
    build: .
    container_name: oriental_register_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./media:/app/media
      - ./database.db:/app/database.db
    environment:
      - DB_URL=sqlite+aiosqlite:///./database.db
