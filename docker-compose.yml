services:
  bot:
    build: .
    container_name: oriental_register_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./media:/app/media
      - ./data:/app/data
    environment:
      - DB_URL=sqlite+aiosqlite:///./data/database.db
